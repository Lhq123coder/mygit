堆：
1.储存new出来的对象
2.垃圾：没有任何引用指向的对象
  GC垃圾回收器不定时清扫垃圾
   System.gc()可以建议JVM尽快调度GC回收
3.内存泄漏：不再使用的对象没有被及时回收
   对象不再使用将引用设为null
栈：
1.存储正在调用的方法的局部变量
2.调用方法时，会为该方法在栈中分配一块对应的栈帧，战阵中存储局部变量和方法参数
实例变量（类中方法外）默认为零
创建对象时存储在堆中，对象被收回时，也被收回
局部变量：无默认值
存在栈中
方法区：存放类的信息
存储.class文件，方法和静态变量
方法只有一份，通过this区分具体的调用对象